# 淘宝NPM

```bash
# 一次性使用, 添加参数 --registry=https://registry.npmmirror.com
npm install -g pnpm --registry=https://registry.npmmirror.com

# 永久使用, 设置默认registry
npm config set registry https://registry.npmmirror.com
```
